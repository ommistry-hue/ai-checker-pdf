# Shared secret — must match PDF_SERVICE_SECRET in the main ai-checker app
# Generate with: openssl rand -hex 32
PDF_SERVICE_SECRET=your-shared-secret-here

# Port (Render sets this automatically — only needed for local dev)
PORT=3001
